create view public.industrial1 as
select
  b.id,
  null::text as import_timestamp,
  b.div as division,
  b.acqda as acquisition_date_text,
  NULLIF(b.clav, ''::text)::numeric as work_center,
  b.grp as group_code,
  b.rmrp as mrp_controller,
  b.resch as resource,
  b.tp as type,
  NULLIF(b.ordine, ''::text)::numeric as order_number,
  b.materiale as material,
  NULLIF(b.causa, ''::text)::numeric as cause_code,
  b.descrizione as description,
  b.autore as author,
  to_date(b.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(b.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(b.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(b.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  b.valcurr as currency,
  NULLIF(b.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  b.prezzostandard as standard_price,
  to_date(b.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  null::numeric as scrap_value_euro,
  'Boiler Lam'::text as source_table
from
  boilerlam b
union all
select
  a.id,
  null::text as import_timestamp,
  a.div as division,
  a.acqda as acquisition_date_text,
  NULLIF(a.clav, ''::text)::numeric as work_center,
  a.grp as group_code,
  a.rmrp as mrp_controller,
  a.resch as resource,
  a.tp as type,
  NULLIF(a.ordine, ''::text)::numeric as order_number,
  a.materiale as material,
  NULLIF(a.causa, ''::text)::numeric as cause_code,
  a.descrizione as description,
  a.autore as author,
  to_date(a.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(a.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(a.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(a.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  null::text as currency,
  NULLIF(a.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  a.prezzostandard as standard_price,
  to_date(a.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  NULLIF(a.valscrapeuro, ''::text)::numeric as scrap_value_euro,
  'Assiemaggio GTH'::text as source_table
from
  assiemaggiogth a
union all
select
  au.id,
  null::text as import_timestamp,
  au.div as division,
  au.acqda as acquisition_date_text,
  NULLIF(au.clav, ''::text)::numeric as work_center,
  au.grp as group_code,
  au.rmrp as mrp_controller,
  au.resch as resource,
  au.tp as type,
  NULLIF(au.ordine, ''::text)::numeric as order_number,
  au.materiale as material,
  NULLIF(au.causa, ''::text)::numeric as cause_code,
  au.descrizione as description,
  au.autore as author,
  to_date(au.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(au.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(au.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(au.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  au.valcurr as currency,
  NULLIF(au.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  au.prezzostandard as standard_price,
  to_date(au.acquisito, 'DD.MM.YYYY'::text) as acquired,
  au.controlstandardpricescrapvalue as control_standard_price,
  null::numeric as scrap_value_euro,
  'Automotive'::text as source_table
from
  automotive au
union all
select
  c.id,
  null::text as import_timestamp,
  c.div as division,
  c.acqda as acquisition_date_text,
  NULLIF(c.clav, ''::text)::numeric as work_center,
  c.grp as group_code,
  c.rmrp as mrp_controller,
  c.resch as resource,
  c.tp as type,
  NULLIF(c.ordine, ''::text)::numeric as order_number,
  c.materiale as material,
  NULLIF(c.causa, ''::text)::numeric as cause_code,
  c.descrizione as description,
  c.autore as author,
  to_date(c.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(c.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(c.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(c.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  c.valcurr as currency,
  NULLIF(c.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  c.prezzostandard as standard_price,
  to_date(c.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  null::numeric as scrap_value_euro,
  'CAM GTH'::text as source_table
from
  camgth c
union all
select
  ct.id,
  null::text as import_timestamp,
  ct.div as division,
  ct.acqda as acquisition_date_text,
  NULLIF(ct.clav, ''::text)::numeric as work_center,
  ct.grp as group_code,
  ct.rmrp as mrp_controller,
  ct.resch as resource,
  ct.tp as type,
  NULLIF(ct.ordine, ''::text)::numeric as order_number,
  ct.materiale as material,
  NULLIF(ct.causa, ''::text)::numeric as cause_code,
  ct.descrizione as description,
  ct.autore as author,
  to_date(ct.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(ct.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(ct.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(ct.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  ct.valcurr as currency,
  NULLIF(ct.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  ct.prezzostandard as standard_price,
  to_date(ct.acquisito, 'DD.MM.YYYY'::text) as acquired,
  ct.controlstandardpricescrapvalue as control_standard_price,
  null::numeric as scrap_value_euro,
  'Cartucce'::text as source_table
from
  cartucce ct
union all
select
  e.id,
  null::text as import_timestamp,
  e.div as division,
  e.acqda as acquisition_date_text,
  NULLIF(e.clav, ''::text)::numeric as work_center,
  e.grp as group_code,
  e.rmrp as mrp_controller,
  e.resch as resource,
  e.tp as type,
  NULLIF(e.ordine, ''::text)::numeric as order_number,
  e.materiale as material,
  NULLIF(e.causa, ''::text)::numeric as cause_code,
  e.descrizione as description,
  e.autore as author,
  to_date(e.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(e.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(e.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(e.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  e.valcurr as currency,
  NULLIF(e.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  e.prezzostandard as standard_price,
  to_date(e.acquisito, 'DD.MM.YYYY'::text) as acquired,
  e.controlstandardpricescrapvalue as control_standard_price,
  null::numeric as scrap_value_euro,
  'Eltplat'::text as source_table
from
  eltplat e
union all
select
  ef.id,
  null::text as import_timestamp,
  ef.div as division,
  ef.acqda as acquisition_date_text,
  NULLIF(ef.clav, ''::text)::numeric as work_center,
  ef.grp as group_code,
  ef.rmrp as mrp_controller,
  ef.resch as resource,
  ef.tp as type,
  NULLIF(ef.ordine, ''::text)::numeric as order_number,
  ef.materiale as material,
  NULLIF(ef.causa, ''::text)::numeric as cause_code,
  ef.descrizione as description,
  ef.autore as author,
  to_date(ef.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(ef.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(ef.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(ef.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  ef.valcurr as currency,
  NULLIF(ef.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  ef.prezzostandard as standard_price,
  to_date(ef.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  null::numeric as scrap_value_euro,
  'Etcfoil'::text as source_table
from
  etcfoil ef
union all
select
  f.id,
  null::text as import_timestamp,
  f.div as division,
  f.acqda as acquisition_date_text,
  NULLIF(f.clav, ''::text)::numeric as work_center,
  f.grp as group_code,
  f.rmrp as mrp_controller,
  f.resch as resource,
  f.tp as type,
  NULLIF(f.ordine, ''::text)::numeric as order_number,
  f.materiale as material,
  NULLIF(f.causa, ''::text)::numeric as cause_code,
  f.descrizione as description,
  f.autore as author,
  to_date(f.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(f.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(f.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(f.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  null::text as currency,
  NULLIF(f.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  f.prezzostandard as standard_price,
  to_date(f.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  NULLIF(f.valscarpeuro, ''::text)::numeric as scrap_value_euro,
  'Finitura GTH'::text as source_table
from
  fingth f
union all
select
  p.id,
  null::text as import_timestamp,
  p.div as division,
  p.acqda as acquisition_date_text,
  NULLIF(p.clav, ''::text)::numeric as work_center,
  p.grp as group_code,
  p.rmrp as mrp_controller,
  p.resch as resource,
  p.tp as type,
  NULLIF(p.ordine, ''::text)::numeric as order_number,
  p.materiale as material,
  NULLIF(p.causa, ''::text)::numeric as cause_code,
  p.descrizione as description,
  p.autore as author,
  to_date(p.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(p.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(p.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(p.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  p.valcurr as currency,
  NULLIF(p.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  p.prezzostandard as standard_price,
  to_date(p.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  null::numeric as scrap_value_euro,
  'Pie GTH'::text as source_table
from
  piegth p
union all
select
  r.id,
  null::text as import_timestamp,
  r.div as division,
  r.acqda as acquisition_date_text,
  NULLIF(r.clav, ''::text)::numeric as work_center,
  r.grp as group_code,
  r.rmrp as mrp_controller,
  r.resch as resource,
  r.tp as type,
  NULLIF(r.ordine, ''::text)::numeric as order_number,
  r.materiale as material,
  NULLIF(r.causa, ''::text)::numeric as cause_code,
  r.descrizione as description,
  r.autore as author,
  to_date(r.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(r.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(r.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(r.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  r.valcurr as currency,
  NULLIF(r.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  r.prezzostandard as standard_price,
  to_date(r.acquisito, 'DD.MM.YYYY'::text) as acquired,
  r.controlstandardpricescrapvalue as control_standard_price,
  null::numeric as scrap_value_euro,
  'RID'::text as source_table
from
  rid r
union all
select
  rs.id,
  null::text as import_timestamp,
  rs.div as division,
  rs.acqda as acquisition_date_text,
  NULLIF(rs.clav, ''::text)::numeric as work_center,
  rs.grp as group_code,
  rs.rmrp as mrp_controller,
  rs.resch as resource,
  rs.tp as type,
  NULLIF(rs.ordine, ''::text)::numeric as order_number,
  rs.materiale as material,
  NULLIF(rs.causa, ''::text)::numeric as cause_code,
  rs.descrizione as description,
  rs.autore as author,
  to_date(rs.datareg, 'DD.MM.YYYY'::text) as registration_date,
  to_date(rs.dataacq, 'DD.MM.YYYY'::text) as acquisition_date,
  NULLIF(rs.qty_scrap, ''::text)::numeric as scrap_quantity,
  NULLIF(rs.confscrapval, ''::text)::numeric as confirmed_scrap_value,
  rs.valcurr as currency,
  NULLIF(rs.confscrapval_1, ''::text)::numeric as confirmed_scrap_value_secondary,
  rs.prezzostandard as standard_price,
  to_date(rs.acquisito, 'DD.MM.YYYY'::text) as acquired,
  null::text as control_standard_price,
  null::numeric as scrap_value_euro,
  'RS'::text as source_table
from
  rs;